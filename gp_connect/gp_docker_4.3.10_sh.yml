      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: kevinmtrowbridge/gpdb-docker
      inputs:
      - name: src-code
      run:
        path: sh
        args:
        - -exc
        - |	
          date
          source /usr/local/greenplum-db/greenplum_path.sh
          gpstate -d /data/master/gpseg-1 -l /home/gpadmin/state_log.log
          /usr/local/greenplum-db/bin/psql -c "select current_date"
        user: gpadmin
